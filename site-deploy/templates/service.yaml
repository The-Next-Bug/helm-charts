{{- range .Values.domains }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-name" . }}-service
spec:
  selector:
    app: {{ include "kube-name" . }}-static
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080 
{{- end }}
